# Shared Layer
TẦNG 3 — Domain logic & UI dùng chung.
Chứa các thành phần (components), hàm tiện ích (utils), và hằng số (constants) có kiến thức về nghiệp vụ hệ thống.
Import được: core/
KHÔNG import: modules/, app/
